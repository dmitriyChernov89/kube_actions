apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "betterme.fullname" . }}-test-connection"
  labels:
    {{- include "betterme.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "betterme.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
